version: '3'

services:

  mongo:
    image: mongo
    container_name: mongo
    ports:
      - 27017:27017


  mongo-express:
    image: mongo-express
    container_name: mongo-express
    ports:
      - 8081:8081